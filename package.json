{
  "name": "flexilla",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "description": "Flexilla - Flexible UI Components Library",
  "workspaces": [
    "packages/*",
    "docs"
  ],
  "scripts": {
    "build": "bun run --filter 'flexipop' build && bun run --filter '@flexilla/*' build",
    "clean": "bun run --filter 'flexipop' clean && bun run --filter '@flexilla/*' clean",
    "bump": "node scripts/bump-versions.mjs",
    "publish:all": "node scripts/publish-all.mjs",
    "release": "bun run build && bun run typecheck && bun run release:publish",
    "test": "bun test",
    "typecheck": "bun run --filter 'flexipop' typecheck && bun run --filter '@flexilla/*' typecheck"
  },
  "devDependencies": {
    "@tsdown/css": "0.21.4",
    "@types/node": "25.5.0",
    "jsdom": "^26.1.0",
    "tsdown": "0.21.4",
    "typescript": "5.9.3",
    "vitest": "4.1.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
