---
import type { HTMLAttributes } from "astro/types"
import { cn } from "@lib/utils"

interface Props extends HTMLAttributes<"tr"> {}

const { class: className, ...rest } = Astro.props
---

<tr class={`${cn("", className)}`} {...rest}>
  <slot />
</tr>