---
import type { HTMLAttributes } from "astro/types"
import { cn } from "@lib/utils"

interface Props extends HTMLAttributes<"th"> {}

const { class: className, ...rest } = Astro.props
---

<th class={`${cn("", className)}`} {...rest}>
  <slot />
</th>