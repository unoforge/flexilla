---
import type { HTMLAttributes } from "astro/types"
import { cn } from "@lib/utils"

interface Props extends HTMLAttributes<"td"> {}

const { class: className, ...rest } = Astro.props
---

<td class={`${cn("", className)}`} {...rest}>
  <slot />
</td>